job('toJar') {
    scm {
        git('git://github.com/Beletskyy/HadoopTestFacilityAvro.git')
    }
    triggers {
        scm('H/2 * * * *')
    }
    steps {
        gradle('build jar')
    }
}
