job('hadoop') {
    scm {
        git('git://github.com/Beletskyy/HadoopTestFacilityAvro')
    }
    triggers {
        scm('* * * * *')
    }
    steps {

        gradle('clean build')
    }
}
