job('build') {
    scm {
        git('git://github.com/Beletskyy/HadoopTestFacilityAvro')
    }
    triggers {
        scm('* * * * *')
    }
    steps {

        gradle('clean checkstyleMain pmdMain findbugsMain build')
        
    }
}
